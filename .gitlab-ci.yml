image: node

before_script:
    - npm install

cache:
    paths:
        - node_modules/

pages:
    script:
        - npm run generate
        - gzip -k -6 -r public
    artifacts:
        paths:
            - public
    only:
        - master
